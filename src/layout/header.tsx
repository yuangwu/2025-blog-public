// 'use client' 是 Next.js 13+ 的客户端指令，表示该文件及其依赖在浏览器端运行（客户端组件）
'use client'

// 定义并默认导出一个名为 Header 的 React 函数组件
export default function Header() {
  // 返回一个空的 <header> 语义化 HTML 标签。实际项目中可在此填充导航栏、Logo 等内容
  return <header></header>
}