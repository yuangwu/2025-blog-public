dist/
# 构建输出目录，通常由打包工具生成，不需要纳入版本控制
node_modules/
# 依赖包目录，可通过包管理器根据 lock 文件重新生成
.next/
# Next.js 框架的默认构建输出目录，临时或缓存文件

**/.git
# 递归匹配所有子目录中的 .git 元数据文件夹（通常不会出现，但用于避免意外提交）
**/.svn
# 递归匹配所有子目录中的 .svn 版本控制文件夹
**/.hg
# 递归匹配所有子目录中的 .hg 版本控制文件夹
**/node_modules
# 递归匹配所有子目录中的 node_modules 依赖目录
**/package-lock.json
# 递归匹配所有子目录中的 npm 锁定文件（视团队策略，有时会提交）
**/pnpm-lock.json
# 递归匹配所有子目录中的 pnpm 锁定文件（通常建议提交，但此处选择忽略）
